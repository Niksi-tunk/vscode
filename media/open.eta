<% layout("./layout.eta") %>

<div class="mx-8">
    <h1>My projects</h1>
    <div class="projects">
    <% it.projects.forEach(p => { %>
        <button onClick=buttonHandler("<%~ p %>") type="button" id="project-button">
            <%= p %>
        </button>
    <% }) %>
    </div>
</div> 
